import React from "react";
import styled from "styled-components/native";

const Container = styled.SafeAreaView`
  flex: 1;
`;

const ClubEditMembers = () => {
  return <Container></Container>;
};

export default ClubEditMembers;
